<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CustomerDAO">


	<select id="infoedit" parameterType="customer"
		resultType="customer">
		SELECT c_id, c_nick, c_pw, c_name, c_tel, c_add, dob
		from customer
		where c_id = #{id}
	</select>

	<update id="CustomerUpdate" parameterType="customer">
		update customer set
		password = #{password},
		name = #{name},
		role = #{role}
		where id
		= #{id}

	</update>






</mapper>